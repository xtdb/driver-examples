{
  "name": "XTDB Driver Examples",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspaces/driver-examples",
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],
  "hostRequirements": {
   "cpus": 4,
   "memory": "4gb",
   "storage": "64gb"
  },

  "forwardPorts": [3000, 3909],
  "portsAttributes": {
    "3000": {
      "label": "Metabase",
      "onAutoForward": "notify"
    },
    "3909": {
      "label": "Garage WebUI",
      "onAutoForward": "notify"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "mtxr.sqltools",
        "mtxr.sqltools-driver-pg",
        "mtxr.sqltools-driver-mysql",
        "betterthantomorrow.calva"
      ],
      "settings": {
        "sqltools.connections": [
          {
            "previewLimit": 50,
            "server": "xtdb",
            "port": 5432,
            "driver": "PostgreSQL",
            "name": "XTDB",
            "database": "xtdb",
            "username": "xtdb",
            "password": "xtdb"
          }
        ],
        "sqltools.results.reuseTabs": "connection"
      },
      "calva.replConnectSequences": [
          {
            "name": "XTDB Clojure Driver Example",
            "projectType": "deps.edn",
            "cljsType": "none",
            "nReplPortFile": [
              ".nrepl-port"
            ],
            "menuSelections": {
              "cljAliases": [
                "dev"
              ]
            }
          }
        ]
    }
  }
}
