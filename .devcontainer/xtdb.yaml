log: !Local
  path: "/var/lib/xtdb/log"

storage: !Local
  path: "/var/lib/xtdb/buffers"

healthz:
  port: 8080
  host: '*'

modules:
- !HttpServer
  port: 3000
  host: '*'

server:
  port: 5432
  host: '*'
